PORT=3000

# Neon / hosted PostgreSQL. Paste the full connection string from Neon here.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST.neon.tech/DATABASE?sslmode=require

# Local PostgreSQL fallback config.
PGUSER=postgres
PGHOST=localhost
PGDATABASE=world
PGPASSWORD=your_local_password
PGPORT=5433
